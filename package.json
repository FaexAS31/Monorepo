{
    "name": "order-system",
    "version": "1.0.0",
    "private": true,
    "engines": {
        "node": ">=20.0.0",
        "pnpm": ">=10.0.0"
    },
    "scripts": {
        "dev": "pnpm --parallel -r dev",
        "build": "pnpm -r build",
        "lint": "pnpm -r lint",
        "clean": "pnpm -r clean"
    },
    "devDependencies": {
        "@types/node": "^25.9.1",
        "typescript": "^5.4.0"
    }
}
